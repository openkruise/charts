{{/* Code generated by agentio-chart-exporter. DO NOT EDIT. */}}
{{- if .Values.agentio.enabled }}
{{- if .Values.agentio.ambient.enabled }}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    {{- include "agentio.cni.labels" . | nindent 4 }}
  name: {{ include "agentio.cni.daemonsetName" . }}
  namespace: {{ include "agentio.namespace" . }}
spec:
  selector:
    matchLabels:
      {{- include "agentio.cni.selectorLabels" . | nindent 6 }}
  {{- with .Values.agentio.ambient.cni.updateStrategy }}
  updateStrategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  template:
    metadata:
      labels:
        {{- include "agentio.cni.labels" . | nindent 8 }}
        sidecar.istio.io/inject: "false"
        istio.io/dataplane-mode: none
      annotations:
        sidecar.istio.io/inject: "false"
        prometheus.io/scrape: "true"
        prometheus.io/port: "15014"
        prometheus.io/path: "/metrics"
        container.apparmor.security.beta.kubernetes.io/install-cni: unconfined
    spec:
      {{- if .Values.agentio.ambient.cni.shareHostNetworkNamespace }}
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      {{- end }}
      nodeSelector:
        kubernetes.io/os: linux
      {{- with .Values.agentio.ambient.cni.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      priorityClassName: system-node-critical
      serviceAccountName: {{ include "agentio.cni.name" . }}
      terminationGracePeriodSeconds: {{ .Values.agentio.ambient.cni.terminationGracePeriodSeconds }}
      containers:
      - name: install-cni
        image: {{ include "agentio.image" (dict "hub" .Values.agentio.ambient.cni.image.hub "name" .Values.agentio.ambient.cni.image.name "tag" .Values.agentio.ambient.cni.image.tag "globalHub" .Values.agentio.global.hub) }}
        ports:
        - containerPort: 15014
          name: metrics
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8000
        securityContext:
          privileged: false
          runAsGroup: 0
          runAsUser: 0
          runAsNonRoot: false
          capabilities:
            drop:
            - ALL
            add:
            - NET_ADMIN
            - NET_RAW
            - SYS_PTRACE
            - SYS_ADMIN
            - DAC_OVERRIDE
        command: ["install-cni"]
        envFrom:
        - configMapRef:
            name: {{ include "agentio.cni.configName" . }}
        env:
        - name: REPAIR_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: REPAIR_RUN_AS_DAEMON
          value: "true"
        - name: REPAIR_SIDECAR_ANNOTATION
          value: "sidecar.istio.io/status"
        {{- if not .Values.agentio.ambient.cni.shareHostNetworkNamespace }}
        - name: ALLOW_SWITCH_TO_HOST_NS
          value: "true"
        {{- end }}
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: spec.nodeName
        - name: GOMEMLIMIT
          valueFrom:
            resourceFieldRef:
              resource: limits.memory
              divisor: "1"
        - name: GOMAXPROCS
          valueFrom:
            resourceFieldRef:
              resource: limits.cpu
              divisor: "1"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        volumeMounts:
        - mountPath: /host/opt/cni/bin
          name: cni-bin-dir
        - mountPath: /host/proc
          name: cni-host-procfs
          readOnly: true
        - mountPath: /host/etc/cni/net.d
          name: cni-net-dir
        - mountPath: /var/run/istio-cni
          name: cni-socket-dir
        - mountPath: /host/var/run/netns
          mountPropagation: HostToContainer
          name: cni-netns-dir
        - mountPath: /var/run/ztunnel
          name: cni-ztunnel-sock-dir
        {{- if .Values.agentio.ambient.cni.resources }}
        resources:
          {{- toYaml .Values.agentio.ambient.cni.resources | nindent 10 }}
        {{- end }}
      volumes:
      - name: cni-bin-dir
        hostPath:
          path: {{ .Values.agentio.ambient.cni.cniBinDir }}
      - name: cni-host-procfs
        hostPath:
          path: /proc
          type: Directory
      - name: cni-ztunnel-sock-dir
        hostPath:
          path: /var/run/ztunnel
          type: DirectoryOrCreate
      - name: cni-net-dir
        hostPath:
          path: {{ .Values.agentio.ambient.cni.cniConfDir }}
      - name: cni-socket-dir
        hostPath:
          path: /var/run/istio-cni
      - name: cni-netns-dir
        hostPath:
          path: {{ .Values.agentio.ambient.cni.cniNetnsDir }}
          type: DirectoryOrCreate
{{- end }}
{{- end }}
