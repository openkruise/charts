{{/* Code generated by agentio-chart-exporter. DO NOT EDIT. */}}
{{- define "agentio.meshConfig.defaults" }}
ingressControllerMode: "OFF"
defaultConfig:
  discoveryAddress: {{ include "agentio.controller.name" . }}.{{ include "agentio.namespace" . }}.svc:15012
defaultProviders:
  accessLogging:
  - envoy
  metrics:
  - prometheus
extensionProviders:
- name: envoy
  envoyFileAccessLog:
    path: /dev/stdout
    omitEmptyValues: true
    logFormat:
      labels:
        authority_for: '%REQ(:AUTHORITY)%'
        bytes_received: '%BYTES_RECEIVED%'
        bytes_sent: '%BYTES_SENT%'
        downstream_address: '%DOWNSTREAM_REMOTE_ADDRESS%'
        duration: '%DURATION%'
        method: '%REQ(:METHOD)%'
        path: '%REQ(X-ENVOY-ORIGINAL-PATH?:PATH)%'
        protocol: '%PROTOCOL%'
        request_id: '%REQ(X-REQUEST-ID)%'
        requested_server_name: '%REQUESTED_SERVER_NAME%'
        response_code: '%RESPONSE_CODE%'
        response_flags: '%RESPONSE_FLAGS%'
        start_time: '%START_TIME%'
        trace_id: '%TRACE_ID%'
        upstream_address: '%DOWNSTREAM_LOCAL_ADDRESS%'
        transport_failure_reason: '%UPSTREAM_TRANSPORT_FAILURE_REASON%'
        user_agent: '%REQ(USER-AGENT)%'
        sandbox_name: '%CEL(filter_state[''downstream_peer''].name)%'
        sandbox_namespace: '%CEL(filter_state[''downstream_peer''].namespace)%'
enablePrometheusMerge: true
rootNamespace: {{ include "agentio.namespace" . }}
trustDomain: {{ .Values.agentio.global.trustDomain }}
{{- end }}
{{- if .Values.agentio.enabled }}


{{/* Merge built-in defaults above with user overrides from .Values.meshConfig. */}}
{{/* The intent is that meshConfig.foo becomes the raw API rather than re-modeling every field in values.yaml. */}}
{{- $defaults := include "agentio.meshConfig.defaults" . | fromYaml }}
{{- $mesh := mergeOverwrite $defaults (.Values.agentio.meshConfig | default dict) }}
apiVersion: v1
data:
  mesh: |-
{{ $mesh | toYaml | indent 4 }}
  meshNetworks: 'networks: {}'
kind: ConfigMap
metadata:
  labels:
    {{- include "agentio.controller.labels" . | nindent 4 }}
  name: {{ .Values.agentio.global.resourcePrefix }}
  namespace: {{ include "agentio.namespace" . }}
{{- end }}
