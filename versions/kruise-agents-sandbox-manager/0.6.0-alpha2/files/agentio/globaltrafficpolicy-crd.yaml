---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: globaltrafficpolicies.agents.kruise.io
spec:
  group: agents.kruise.io
  names:
    kind: GlobalTrafficPolicy
    listKind: GlobalTrafficPolicyList
    plural: globaltrafficpolicies
    shortNames:
    - gtp
    singular: globaltrafficpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GlobalTrafficPolicy defines bidirectional traffic rules cluster-wide.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TrafficPolicySpec defines bidirectional policy state on selected
              pods.
            properties:
              egress:
                description: Egress defines rules applied to outbound traffic of selected
                  pods.
                properties:
                  rules:
                    description: Rules is the ordered rule list for this direction.
                    items:
                      description: |-
                        TrafficPolicyRule is one entry in the ordered rule list. Each rule
                        specifies an action (allow/reject) and optional peer and port constraints.
                        When From/To/Ports are all empty, the action applies to all traffic in
                        that direction.
                      properties:
                        action:
                          description: Action determines whether matched traffic is
                            allowed or denied.
                          enum:
                          - allow
                          - reject
                          type: string
                        from:
                          description: From lists source peers. Multiple entries are
                            ORed.
                          items:
                            description: |-
                              TrafficPolicyPeer identifies a traffic source or destination. Exactly one
                              of CIDR, FQDN, Service, or Workload must be set.
                            properties:
                              cidr:
                                description: CIDR is an IP address range in CIDR notation
                                  (e.g. "10.0.0.0/8").
                                maxLength: 43
                                minLength: 1
                                type: string
                              fqdn:
                                description: FQDN is a fully qualified domain name
                                  to match (e.g. "api.example.com").
                                maxLength: 253
                                minLength: 1
                                type: string
                              service:
                                description: Service references a Kubernetes Service
                                  and its selected endpoints.
                                properties:
                                  name:
                                    description: Name is the Service resource name.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace is the namespace of the target Service. When omitted, defaults
                                      to the namespace of the enclosing TrafficPolicy.
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                required:
                                - name
                                type: object
                              workload:
                                description: |-
                                  Workload selects pods by namespace and labels; their IP addresses form
                                  the peer address set.
                                properties:
                                  namespace:
                                    description: Namespace of the target pods.
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                  selector:
                                    additionalProperties:
                                      type: string
                                    description: Selector is a label selector that
                                      matches the target pods.
                                    maxProperties: 10
                                    type: object
                                required:
                                - namespace
                                - selector
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: exactly one of cidr, fqdn, service, or workload
                                must be set
                              rule: '[has(self.cidr), has(self.fqdn), has(self.service),
                                has(self.workload)].filter(x, x).size() == 1'
                          maxItems: 20
                          type: array
                          x-kubernetes-list-type: atomic
                        ports:
                          description: |-
                            Ports restricts this rule to specific L4 protocol/port combinations.
                            Multiple entries are ORed. If empty, the rule matches all ports.
                          items:
                            description: |-
                              TrafficPolicyPort restricts a rule to specific protocol/port combinations.
                              If Protocol is non-empty and Port is nil, matches all ports of that protocol.
                            properties:
                              endPort:
                                description: |-
                                  EndPort defines the upper bound of a port range (inclusive). When set,
                                  the rule matches destination ports from Port to EndPort. Requires Port
                                  to be set and must be >= Port.
                                format: int32
                                maximum: 65535
                                minimum: 1
                                type: integer
                              port:
                                description: |-
                                  Port is the destination port number. When nil, the rule applies to all
                                  TCP ports.
                                format: int32
                                maximum: 65535
                                minimum: 1
                                type: integer
                              protocol:
                                enum:
                                - TCP
                                - UDP
                                - ICMP
                                - SCTP
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: endPort requires port to be set
                              rule: '!has(self.endPort) || has(self.port)'
                            - message: endPort must be greater than or equal to port
                              rule: '!has(self.endPort) || self.endPort >= self.port'
                          maxItems: 20
                          type: array
                          x-kubernetes-list-type: atomic
                        to:
                          description: To lists destination peers. Multiple entries
                            are ORed.
                          items:
                            description: |-
                              TrafficPolicyPeer identifies a traffic source or destination. Exactly one
                              of CIDR, FQDN, Service, or Workload must be set.
                            properties:
                              cidr:
                                description: CIDR is an IP address range in CIDR notation
                                  (e.g. "10.0.0.0/8").
                                maxLength: 43
                                minLength: 1
                                type: string
                              fqdn:
                                description: FQDN is a fully qualified domain name
                                  to match (e.g. "api.example.com").
                                maxLength: 253
                                minLength: 1
                                type: string
                              service:
                                description: Service references a Kubernetes Service
                                  and its selected endpoints.
                                properties:
                                  name:
                                    description: Name is the Service resource name.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace is the namespace of the target Service. When omitted, defaults
                                      to the namespace of the enclosing TrafficPolicy.
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                required:
                                - name
                                type: object
                              workload:
                                description: |-
                                  Workload selects pods by namespace and labels; their IP addresses form
                                  the peer address set.
                                properties:
                                  namespace:
                                    description: Namespace of the target pods.
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                  selector:
                                    additionalProperties:
                                      type: string
                                    description: Selector is a label selector that
                                      matches the target pods.
                                    maxProperties: 10
                                    type: object
                                required:
                                - namespace
                                - selector
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: exactly one of cidr, fqdn, service, or workload
                                must be set
                              rule: '[has(self.cidr), has(self.fqdn), has(self.service),
                                has(self.workload)].filter(x, x).size() == 1'
                          maxItems: 20
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - action
                      type: object
                    maxItems: 50
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              ingress:
                description: Ingress defines rules applied to inbound traffic of selected
                  pods.
                properties:
                  rules:
                    description: Rules is the ordered rule list for this direction.
                    items:
                      description: |-
                        TrafficPolicyRule is one entry in the ordered rule list. Each rule
                        specifies an action (allow/reject) and optional peer and port constraints.
                        When From/To/Ports are all empty, the action applies to all traffic in
                        that direction.
                      properties:
                        action:
                          description: Action determines whether matched traffic is
                            allowed or denied.
                          enum:
                          - allow
                          - reject
                          type: string
                        from:
                          description: From lists source peers. Multiple entries are
                            ORed.
                          items:
                            description: |-
                              TrafficPolicyPeer identifies a traffic source or destination. Exactly one
                              of CIDR, FQDN, Service, or Workload must be set.
                            properties:
                              cidr:
                                description: CIDR is an IP address range in CIDR notation
                                  (e.g. "10.0.0.0/8").
                                maxLength: 43
                                minLength: 1
                                type: string
                              fqdn:
                                description: FQDN is a fully qualified domain name
                                  to match (e.g. "api.example.com").
                                maxLength: 253
                                minLength: 1
                                type: string
                              service:
                                description: Service references a Kubernetes Service
                                  and its selected endpoints.
                                properties:
                                  name:
                                    description: Name is the Service resource name.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace is the namespace of the target Service. When omitted, defaults
                                      to the namespace of the enclosing TrafficPolicy.
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                required:
                                - name
                                type: object
                              workload:
                                description: |-
                                  Workload selects pods by namespace and labels; their IP addresses form
                                  the peer address set.
                                properties:
                                  namespace:
                                    description: Namespace of the target pods.
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                  selector:
                                    additionalProperties:
                                      type: string
                                    description: Selector is a label selector that
                                      matches the target pods.
                                    maxProperties: 10
                                    type: object
                                required:
                                - namespace
                                - selector
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: exactly one of cidr, fqdn, service, or workload
                                must be set
                              rule: '[has(self.cidr), has(self.fqdn), has(self.service),
                                has(self.workload)].filter(x, x).size() == 1'
                          maxItems: 20
                          type: array
                          x-kubernetes-list-type: atomic
                        ports:
                          description: |-
                            Ports restricts this rule to specific L4 protocol/port combinations.
                            Multiple entries are ORed. If empty, the rule matches all ports.
                          items:
                            description: |-
                              TrafficPolicyPort restricts a rule to specific protocol/port combinations.
                              If Protocol is non-empty and Port is nil, matches all ports of that protocol.
                            properties:
                              endPort:
                                description: |-
                                  EndPort defines the upper bound of a port range (inclusive). When set,
                                  the rule matches destination ports from Port to EndPort. Requires Port
                                  to be set and must be >= Port.
                                format: int32
                                maximum: 65535
                                minimum: 1
                                type: integer
                              port:
                                description: |-
                                  Port is the destination port number. When nil, the rule applies to all
                                  TCP ports.
                                format: int32
                                maximum: 65535
                                minimum: 1
                                type: integer
                              protocol:
                                enum:
                                - TCP
                                - UDP
                                - ICMP
                                - SCTP
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: endPort requires port to be set
                              rule: '!has(self.endPort) || has(self.port)'
                            - message: endPort must be greater than or equal to port
                              rule: '!has(self.endPort) || self.endPort >= self.port'
                          maxItems: 20
                          type: array
                          x-kubernetes-list-type: atomic
                        to:
                          description: To lists destination peers. Multiple entries
                            are ORed.
                          items:
                            description: |-
                              TrafficPolicyPeer identifies a traffic source or destination. Exactly one
                              of CIDR, FQDN, Service, or Workload must be set.
                            properties:
                              cidr:
                                description: CIDR is an IP address range in CIDR notation
                                  (e.g. "10.0.0.0/8").
                                maxLength: 43
                                minLength: 1
                                type: string
                              fqdn:
                                description: FQDN is a fully qualified domain name
                                  to match (e.g. "api.example.com").
                                maxLength: 253
                                minLength: 1
                                type: string
                              service:
                                description: Service references a Kubernetes Service
                                  and its selected endpoints.
                                properties:
                                  name:
                                    description: Name is the Service resource name.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace is the namespace of the target Service. When omitted, defaults
                                      to the namespace of the enclosing TrafficPolicy.
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                required:
                                - name
                                type: object
                              workload:
                                description: |-
                                  Workload selects pods by namespace and labels; their IP addresses form
                                  the peer address set.
                                properties:
                                  namespace:
                                    description: Namespace of the target pods.
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                  selector:
                                    additionalProperties:
                                      type: string
                                    description: Selector is a label selector that
                                      matches the target pods.
                                    maxProperties: 10
                                    type: object
                                required:
                                - namespace
                                - selector
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: exactly one of cidr, fqdn, service, or workload
                                must be set
                              rule: '[has(self.cidr), has(self.fqdn), has(self.service),
                                has(self.workload)].filter(x, x).size() == 1'
                          maxItems: 20
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - action
                      type: object
                    maxItems: 50
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              priority:
                default: 1000
                description: |-
                  Priority determines the evaluation order when multiple TrafficPolicies
                  match the same pod. Higher values are evaluated first. When two
                  policies share the same priority, the result is implementation-defined.
                format: int32
                minimum: 0
                type: integer
              selector:
                description: |-
                  Selector chooses the pods this policy applies to. Standard
                  LabelSelector semantics: an EMPTY selector matches EVERY pod within
                  the policy's scope (namespace for TrafficPolicy, cluster-wide for
                  GlobalTrafficPolicy).
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - selector
            type: object
            x-kubernetes-validations:
            - message: at least one of ingress or egress must be specified
              rule: has(self.ingress) || has(self.egress)
          status:
            description: TrafficPolicyStatus captures the observed state of TrafficPolicy.
            properties:
              conditions:
                description: |-
                  Conditions summarises the policy's current state. Standard types are
                  Accepted and Programmed (see TrafficPolicyCondition* constants).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 20
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
